#include <stdio.h>
#include <stdlib.h>

#define NUM_PROCESS 2
#define BASH_MAX_N 1000

/* * * * * * * * * * * * * * * * *
BENCHMARK CONFIG
 * * * * * * * * * * * * * * * * */ 

// Java Benchmark
#define TNAME_JAVA "Java Benchmark"
#define 

typedef struct {
     const char* name;
     const char* exec_path;
     char* const* args;
} Test;

const Test* getTests() {
     
}

int main() {
     
     
}